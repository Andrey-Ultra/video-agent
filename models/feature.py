from abc import ABC, abstractmethod
from typing import Any


class Feature(ABC):
    """
    Базовый интерфейс для любой характеристики кадра/видео,
    которую можно извлечь и сравнить с такой же характеристикой
    другого объекта. Конкретное внутреннее представление
    (числовой вектор, маска силуэта, количество цветов и т.д.)
    определяется в наследниках.
    """

    @abstractmethod
    def distance(self, other: "Feature") -> float:
        """Расстояние между двумя признаками. Чем меньше — тем ближе."""
        ...